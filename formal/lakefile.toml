name = "CclFormal"
defaultTargets = ["CclFormal", "subverdict"]

[[lean_lib]]
name = "CclFormal"

# The M1 differential oracle (see Main.lean). `lake build` produces
# .lake/build/bin/subverdict, which the Rust harness
# (src/ccl/infer/solver/differential.rs) spawns.
[[lean_exe]]
name = "subverdict"
root = "Main"
