a := ""
b := ""

a_reqs, a_resps = http_serve("{PORT}", "POST", "/a")
b_reqs, b_resps = http_serve("{PORT}", "POST", "/b")

for x in a_reqs:
    a := a + x + "\n"
    a_resps << a

for y in b_reqs:
    b := b + y + "\n"
    b_resps << b
