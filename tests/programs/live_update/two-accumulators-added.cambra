left := ""
right := ""
extra := ""

reqs, resps = http_serve("{PORT}", "POST", "/bump")

for x in reqs:
    left := left + "a"
    right := right + "B"
    extra := extra + "c"
    resps << left + "|" + right + "|" + extra + "\n"
