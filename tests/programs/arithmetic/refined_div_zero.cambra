# Seems that the "=> Int" return type annotation
# is currently not parsable.  Docs say that syntax is "tentative".
#
# The refinement syntax here is "decided but not implemented".
def refined_div(left: Int, right: {Int where _ != 0}):
    left // right

y = refined_div(2, 0)

y
